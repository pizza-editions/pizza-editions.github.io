name: IndexNow Submit (Clean)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  indexnow:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests lxml

      - name: Submit Sitemap URLs to IndexNow
        env:
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}
        run: |
          python - <<EOF
          import os, requests, json
          from lxml import etree

          KEY = os.getenv("INDEXNOW_KEY")
          HOST = "pizza-editions.github.io"
          SITEMAP = "https://pizza-editions.github.io/sitemap.xml"
          KEY_URL = f"https://{HOST}/{KEY}.txt"
          ENDPOINT = "https://api.indexnow.org/indexnow"

          # 1. verify key file
          r = requests.head(KEY_URL, timeout=10)
          if r.status_code != 200:
              raise Exception("IndexNow key file NOT reachable")

          # 2. fetch sitemap
          res = requests.get(SITEMAP, timeout=20)
          res.raise_for_status()

          tree = etree.fromstring(res.content)
          ns = {"ns": "http://www.sitemaps.org/schemas/sitemap/0.9"}
          urls = [loc.text.strip() for loc in tree.xpath("//ns:loc", namespaces=ns)]

          if not urls:
              raise Exception("No URLs found in sitemap")

          # 3. submit (chunk safe)
          payload = {
              "host": HOST,
              "key": KEY,
              "keyLocation": KEY_URL,
              "urlList": urls[:10000]
          }

          headers = {"Content-Type": "application/json"}
          resp = requests.post(ENDPOINT, headers=headers, data=json.dumps(payload))

          print("URLs submitted:", len(payload["urlList"]))
          print("Status:", resp.status_code)
          print("Response:", resp.text)
          EOF
